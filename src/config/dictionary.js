import { months } from './translations/months'
import { weekDays } from './translations/weekDays'
import { days } from './translations/postfixes/days'

const MONTHS = { ...months }

const WEEKDAYS = { ...weekDays }

const DAYS = { ...days }

export {
  MONTHS,
  WEEKDAYS,
  DAYS
}
